#version 330 // OpenGL version 3.3 (minimum requirement)

in vec4 retColor; // MUST use the same name as the out variable used by the vertex shader
out vec4 FragColor;

void main()
{
	FragColor = retColor;
}